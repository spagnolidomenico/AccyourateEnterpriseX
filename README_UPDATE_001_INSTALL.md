# Installazione UPDATE-001 - Update Center

Esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit:

```powershell
git add .
git commit -m "UPDATE-001: Add update center foundation"
git push
```
