# Installazione HR-002A - Domain Models

Copia nel repository:

- `src`
- `docs`
- `tests`
- `README_HR_002A_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "HR-002A: Add Human Resources domain models"
git push
```
