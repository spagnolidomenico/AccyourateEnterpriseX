# Installazione 14.1.0D1 - Fix Assign Asset Duplicate

Copia nel repository:

- `src`
- `docs`
- `README_14_1_0D1_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "ASSET-003: Fix duplicate assign asset method"
git push
```
