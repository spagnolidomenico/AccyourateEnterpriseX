# Installazione EP-006

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-adaptive-ui.ps1
```

Commit:

```powershell
git add .
git commit -m "EP-006: Add enterprise adaptive UI framework"
git push
```
