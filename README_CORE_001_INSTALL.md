# Installazione CORE-001 - Warning Cleanup

Esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit:

```powershell
git add .
git commit -m "CORE-001: Clean up build warnings"
git push
```
