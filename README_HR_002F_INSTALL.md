# Installazione HR-002F - Employee Asset Integration

Copia nel repository:

- `src`
- `docs`
- `tests`
- `README_HR_002F_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "HR-002F: Link employee profile with assigned assets"
git push
```
