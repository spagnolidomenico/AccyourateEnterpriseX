# Installazione BRAND-004 - Adaptive Login Logo

Copia nel repository:

- `src`
- `docs`
- `README_BRAND_004_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "BRAND-004: Improve adaptive login logo layout"
git push
```
