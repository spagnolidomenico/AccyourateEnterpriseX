# Installazione 15.0.1C.2 - Notification Center UI

Copia nel repository:

- `src`
- `docs`
- `tests`
- `README_15_0_1C2_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "PLATFORM-002: Add notification center UI"
git push
```
