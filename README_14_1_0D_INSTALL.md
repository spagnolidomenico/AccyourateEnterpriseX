# Installazione 14.1.0D - Asset Assignment UI

Copia nel repository:

- `src`
- `docs`
- `tests`
- `README_14_1_0D_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "ASSET-002: Add asset assignment UI"
git push
```
