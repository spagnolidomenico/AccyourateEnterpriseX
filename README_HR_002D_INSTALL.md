# Installazione HR-002D - Human Resources Workspace

Copia nel repository:

- `src`
- `docs`
- `tests`
- `README_HR_002D_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "HR-002D: Add Human Resources workspace"
git push
```
