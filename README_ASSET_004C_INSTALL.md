# Installazione ASSET-004C - Delivery Report UI

Copia nel repository:

- `src`
- `docs`
- `tests`
- `README_ASSET_004C_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "ASSET-004C: Add delivery report workspace UI"
git push
```
