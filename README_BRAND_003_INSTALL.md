# Installazione BRAND-003 - Login Company Logo

Copia nel repository:

- `src`
- `docs`
- `tests`
- `README_BRAND_003_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "BRAND-003: Add company logo to branded login"
git push
```
