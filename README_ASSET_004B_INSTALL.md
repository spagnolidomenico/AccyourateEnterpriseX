# Installazione ASSET-004B - PDF Engine

Esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit:

```powershell
git add .
git commit -m "ASSET-004B: Add PDF engine for delivery reports"
git push
```
