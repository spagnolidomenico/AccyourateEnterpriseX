# Installazione DOCUMENTS-001

Esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit:

```powershell
git add .
git commit -m "DOCUMENTS-001: Add document center foundation"
git push
```
