# Installazione SETTINGS-001B - Platform Integration

Esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit:

```powershell
git add .
git commit -m "SETTINGS-001B: Integrate settings with PDF and numbering"
git push
```
