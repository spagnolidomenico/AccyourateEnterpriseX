# Installazione SETTINGS-001A - Settings Center Foundation

Copia nel repository:

- `src`
- `scripts`
- `docs`
- `tests`
- `README_SETTINGS_001A_INSTALL.md`

Poi esegui:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-smoke.ps1
```

Commit consigliato:

```powershell
git add .
git commit -m "SETTINGS-001A: Add settings center foundation"
git push
```
